# WARNING ⚠️: this is a Netlify file with local plugin
# so it might look quite different from the user's Netlify configuration
[build]
command = "echo 'Netlify build command ...'"
publish = "public"

[[plugins]]
  package = "."
  [plugins.inputs]
    owner = "bahmutov"
    repo = "netlify-plugin-github-dispatch"
    workflow = ".github/workflows/e2e.yml"
